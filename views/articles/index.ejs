<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Blogs</title>
</head>
<body style="background-color: rgb(190, 233, 236)">
    
    <!-- so here instead of rendering article we are gonna render a text -->
    <!-- <%=  %>  this allows us to o/p a var and this will a js variable from nodejs 
     second if we dont write equal to after % then it will still work but it wont display it on page  -->
     <!-- text muted in line 25 will give greyish color -->
     <!-- When using toLocaleDateString we should make sure that we use new Date in server.js at createdAt -->
    <div class="container">
    
        <!-- mb-4 for margin bottom of 4  -->
        <h1 class="mb-4">Blog Articles</h1> 
        <a href="/articles/new" class="btn btn-success">New Article</a>

        
        <% articles.forEach(articles => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%= articles.title %></h4>
                    <div class="card-subtitle text-muted mb-2"> 
                        <%= articles.createdAt.toLocaleDateString() %>
                    </div>

                    <div class="card-text mb-2"><%= articles.description %></div>
                    <a href="articles/<%= articles.slug %>" class="btn btn-primary">Read More</a>
                    <a href="articles/edit/<%= articles.id %>" class="btn btn-info">Edit</a>
                    <form action="/articles/<%= articles.id %>?_method=DELETE" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-danger">Delete</button>
                    </form>


                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>